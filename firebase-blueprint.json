{
  "entities": {
    "Usuari": {
      "title": "Perfil d'Usuari",
      "description": "Dades de perfil de l'usuari registrat a l'aplicació per al control d'accés i personalització.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "Identificador únic de l'usuari provinent de Firebase Authentication"
        },
        "email": {
          "type": "string",
          "description": "Adreça de correu electrònic de l'usuari utilitzada per iniciar sessió"
        },
        "nom": {
          "type": "string",
          "description": "Nom complet de l'usuari"
        },
        "rol": {
          "type": "string",
          "enum": ["usuari", "admin"],
          "description": "Rol de l'usuari dins de l'aplicació"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Data de creació del perfil de l'usuari"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Data de la darrera actualització del perfil"
        }
      },
      "required": ["uid", "email", "rol"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "Usuari",
      "description": "Secció de perfils d'usuari protegits per l'UID propi de Firebase Auth"
    }
  }
}
